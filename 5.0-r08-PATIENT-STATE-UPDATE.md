# 5.0-r08 全局 Patient State 更新

## 核心变化
- 建立全局患者状态 `vct50_patient_state`。
- 患者物种、品种、年龄、性别、体重、主诉、病史/基础病、肌酐、K、Na、白蛋白、当前用药、工作诊断等信息可在一个入口录入。
- 同步后自动联动剂量/制剂、复溶、麻醉、临床决策中心、病例工作台、快速计算和 AI 临床助手的相关字段。
- 当前用药可自动作为三药相互作用审计的初始输入（仅在审计字段为空时填充，不覆盖医生已有输入）。
- Patient State 保存在浏览器 localStorage，仅在本机使用，不上传服务器。
- 保留 5.0-r08 版本，不进行数据库迁移。

## 临床安全边界
Patient State 只负责参数共享，不自动替代诊断、处方或临床判断。药物剂量仍必须回到具体制剂标签、权威指南、患者状态和医院 protocol 复核。
